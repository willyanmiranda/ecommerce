import OpenCart from "./open-cart";

export default function CartModal() {
    return (
      <>
        <button aria-label="Open cart">
          <OpenCart />
        </button>
      </>
    );
}